// Family Access — the new sharing UI. Replaces the previous Household snapshot
// share screen (logic preserved server-side via household_shared_snapshots,
// which the family viewer reads).
export { FamilyTab as HouseholdTab } from "./FamilyTab";
